# Architecture

> Auto-generated by /map on 2026-02-21

## Overview

A PowerShell-based utility application providing system management functions (Backups, Debloating, Installing, Setup, Updates), driven by a WPF-based graphical user interface (XAML).

```
┌───────────────────────────────────────────┐
│        launcher.ps1 / bootstrap.ps1       │
├───────────────────────────────────────────┤
│    [UI Layer: UIManager.ps1 / Main.xaml]  │
├───────────────────────────────────────────┤
│            [Modules Layer]                │
│ (Backups, Debloater, Installers, Setup...)│
├───────────────────────────────────────────┤
│      [Core Layer: Config, Logger]         │
└───────────────────────────────────────────┘
```

## Components

### Core
- **Purpose**: Foundational utilities like configuration parsing and logging.
- **Location**: `core/` (`ConfigManager.ps1`, `Logger.ps1`)
- **Dependencies**: Native PowerShell commands.

### Modules
- **Purpose**: The business logic for various system actions (e.g., system debloating, package installers, git management, system backups).
- **Location**: `modules/` and `modules/git/`
- **Dependencies**: Consumes the Core utilities and operates on Windows OS APIs.

### UI
- **Purpose**: Graphical frontend using Windows Presentation Foundation (WPF).
- **Location**: `ui/` (`Main.xaml`, `UIManager.ps1`)
- **Dependencies**: WPF, PresentationFramework, core components.

### Scripts
- **Purpose**: Developer tooling, repo validation, searching, etc.
- **Location**: `scripts/`

## Data Flow

1. User launches `launcher.ps1` (or bootstraps using `bootstrap.ps1`).
2. The UI Manager loads `Main.xaml` to construct the visual application.
3. User interactions in the UI trigger functions in `UIManager.ps1`.
4. The `UIManager.ps1` delegates execution to specific scripts in `modules/`.
5. Logs and Configuration are pulled from `core/`.

## Integration Points

| Service | Type | Purpose |
|---------|------|---------|
| WebRequests | HTTP | Downloads payloads (e.g. repo archives or installers) in `Setup.ps1` and `bootstrap.ps1` using `Invoke-WebRequest`. |

## Technical Debt

- None immediately identified in source files.

## Conventions

**Naming:** PascalCase for PowerShell script files (`ConfigManager.ps1`, `Main.xaml`).
**Structure:** Clear separation of concerns (`core`, `modules`, `ui`, `scripts`).
**Testing:** Build/validate shell scripts in `scripts/`.
